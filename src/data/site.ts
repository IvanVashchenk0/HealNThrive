// Compatibility export for the existing shared components.
export { siteContent, navigation } from "./siteContent";
// No verified impact counts were supplied.
export const impactStats: Array<{ value: string; label: string; detail: string }> = [];
